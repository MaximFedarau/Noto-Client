import React, { FC } from 'react';

import Form from '@components/RecordsManaging/Form/Form.component';
import { SafeAreaContainer } from '@components/Default/View';

export const RecordsManaging: FC = () => (
  <SafeAreaContainer>
    <Form />
  </SafeAreaContainer>
);
